load("@bazel_latex//:latex.bzl", "latex_document")

BIB_LATEX = [
    "@bazel_latex//packages:biblatex"
]

latex_document(
    name = "research",
    srcs = glob(["**/*.tex"]) + glob(["*.bib"]) + BIB_LATEX,
    main = "hashgraph-research-assignment.tex",
    cmd_flags = ["--bibtex-cmd=biber"]
)
